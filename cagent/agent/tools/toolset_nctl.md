# Description

Read-only view of this cluster: desired vs actual state, drift, placement
relations, and past operations.

Run `cagent --help` to see the available subcommands, and
`cagent <subcommand> --help` for each one's usage.
