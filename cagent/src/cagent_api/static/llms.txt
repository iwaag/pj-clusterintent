# cluster-agent

An HTTP entrance onto a command node's cluster-agent (an OpenCode agent
session whose working directory is the pj-clusterintent superproject). Ask
it about cluster resources/services — status, drift, relations, "does X
exist", "how do I get S3-compatible storage" — instead of guessing or
installing your own. It reads the cluster's actual state and answers with
guidance. It can also hand you data as a file: ask for e.g. "the cluster's
desired/actual state as a downloadable file" and the reply will contain a
time-limited download URL (fetch it before the stated expiry). Recoverable
desired-state changes and reconciles it can carry out when asked; destroy-class
operations are denied to it at the tool-permission layer and stay with a human.

## There is also a window (no credentials needed)

Besides this authenticated entrance, the same cluster-agent answers on an
**unauthenticated window**, port `:8790` by default: `POST /window
{"text": "..."}`, `GET /guide`, `GET /healthz`, and the same
`GET /requests/{id}` polling. The Cagent bot in Zulip is that door too — a
direct message to it becomes one window request.

Use the window when you have no certificate and no token, when you only need
to read, and — above all — **to report that one of the cluster-agent's
answers was wrong**. "You told me node X was up, it is not" makes the window
record the report as a file and reply with where it went; it does not try to
repair anything in that turn. Ask it "what has been reported lately" to see
recent ones. Its own capability card is `GET /guide` on that port.

The window can only read (`nctl status`/`drift`/`relations`/`actual`/`ops`)
and record incidents. Cluster changes stay on this entrance.

## Endpoints

All bodies are JSON. Requests are async by default: create/continue return
immediately with `state: "queued"`; poll for the result.

- `POST /requests` — start a new session: `{"message": "..."}` ->
  `{"request_id", "session_id", "state"}` (202)
- `POST /sessions/{session_id}/requests` — continue the same session with a
  follow-up turn, same body/response shape
- `GET /requests/{request_id}` — poll for status/result:
  `{"request_id", "session_id", "state", "response", "error", "cost_usd",
  ...}`. `state` is one of `queued | running | completed | failed |
  cancelled | interrupted`; `response` is null until `completed`;
  `cost_usd` is what this turn cost (see "What it costs") and `backend` names
  the harness/provider/model that served it.
- `POST /requests/{request_id}/cancel` — cancel a queued/running request.
  **This is the only way to stop a run.** The worker outlives the caller:
  closing the client, killing curl, or abandoning the poll leaves the turn
  running to completion on the node, where it can still take actions and
  still costs money. Two turn1 requests believed cancelled did exactly
  that — one of them created an LXC guest.
- `GET /sessions` — list sessions you can see
- `GET /sessions/{session_id}/requests` — list a session's requests in order

## Timing

A real answer can take several minutes (multi-step turns have taken up to
~4 minutes) — longer than most shell/tool-call default timeouts. Do not
issue a request and block on it with a short timeout; submit, do something
else, then poll `GET /requests/{request_id}` again a bit later. Give it
several minutes before concluding a request has failed.

## What it costs

This file is the cluster-agent's capability card; the agent answers "what
can you do" and "what does it cost" from it in a session too, so you can
just ask.

- **Money: reported per request.** `GET /requests/{request_id}` returns
  `cost_usd` — what this turn added to its OpenCode session, from OpenCode's
  own accounting. `null` means not measured (the turn never reached the
  backend, or the record predates this field); on the cancel and timeout
  paths it is a floor, not a total, because the turn may still be running.
  Measured 2026-08-10 over 7 sessions on `openai/gpt-5.6-luna`: $0.0025 for
  a capability question, ~$0.005 for a plain read, $0.042 for a multi-step
  composition, $0.062 for a guest creation; median ~$0.005. Cents, not
  dollars. The backend model is `CAGENT_OPENCODE_MODEL` (default
  `openai/gpt-5.6-luna`) and those figures move with it.
- **Time: seconds to ~4 minutes**, as above. A single read
  (`nctl status`, "does X exist") is usually well under a minute; a
  multi-command state bundle is the slow end.
- **Cluster side effects: none unless you asked for them.** Reads and
  `nctl upload` are free of consequence. Recoverable desired-state changes
  and reconciles happen only when requested. Irreversible operations are
  refused outright, whoever asks.
- Uploaded files sit in a local MinIO outbox and their URLs expire (default
  30 minutes, `--ttl` to change).

## Scope

What it can act on is this cluster's desired state and reconciliation, through
`nctl`. Destroy-class commands (`--allow-destroy`, `nctl prune`, the Proxmox
destroy playbooks, `pct`/`qm destroy`, `pvesh delete`, storage erasure) are
denied to it at the tool-permission layer, so an irreversible operation comes
back as a refusal with its reason, whoever asks and however the request is
framed. That layer matches command strings, not identities: it is a narrow
list, not a capability boundary — see
devdocs/episodes/unshackle_agent/clusterintent/turn2/report.md.

It also cannot write files outside `.local/` and the temp directories, so it
answers questions about the repository rather than editing it.

## Authentication (node/agent callers)

The node entrance (this port, `:8788`) requires a client certificate for
every route except this one. If you got here, you have no cert yet, or
curl didn't present one.

Check for an existing enrollment first:

    ls ~/.cagent/{ca_cert.pem,node_cert.pem,node_key.pem,client.conf}

If those files exist, use the `cagent` wrapper already on your PATH
(`cagent ask`, `cagent continue SESSION_ID`, `cagent status REQUEST_ID` —
see `cagent ask --help`), or call curl directly:

    curl --cacert ~/.cagent/ca_cert.pem \
         --cert ~/.cagent/node_cert.pem --key ~/.cagent/node_key.pem \
         -H 'Content-Type: application/json' \
         -d '{"message": "..."}' https://<this-host>:8788/requests

If those files do not exist, this node is not enrolled yet — that is a
one-time, human-driven step (generate a CSR, send it to the cluster
operator to sign and register), not something to script around from here.
Ask the operator to enroll this node, or see
devdocs/vision/cluster_agent/p2/plan.md ("Step 5b") in the
pj-clusterintent repository for the exact procedure.

## Full contract

See devdocs/vision/cluster_agent/{p1,p2,p4}/contract.md in the
pj-clusterintent repository for the authoritative, versioned API contract
(state machine, error shape, identity/ownership rules) — this file is a
short operational summary, not a substitute.
