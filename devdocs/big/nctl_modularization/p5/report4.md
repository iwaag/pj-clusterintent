# P5 Step 4 — Module admission rules

Status: complete.

- Recorded all seven roadmap admission rules in `nctl/README.md` beside the component conventions.
- The README now explicitly defines the four-layer direction as `presentation → orchestration → domain ← transport`, the responsibility-not-line-count rule, and the two-implementation (or approved second implementation) interface rule.
- Documentation-only change; no abstraction, compatibility layer, or framework was introduced.

